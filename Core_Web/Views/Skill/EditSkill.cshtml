@model EntityLayer.Concrete.Skill
@{
    ViewData["Title"] = "EditSkill";
    Layout = "~/Views/Shared/_NewAdminLayout.cshtml";
}

@using (Html.BeginForm("EditSkill","Skill",FormMethod.Post))
{
    @Html.HiddenFor(x=>x.SkillId);
    <div class="form-group">
        @Html.LabelFor(Model=>Model.Title,new {@class="form-control"})
        @Html.TextBoxFor(Model=>Model.Title,new {@class="form-control"})

    </div>
    <div class="form-group">

        @Html.LabelFor(Model=>Model.Value,new {@class="form-control"})
        @Html.TextBoxFor(Model=>Model.Value,new {@class="form-control"})
    </div>
    <button class="btn btn-info">Güncelle</button>
}

